# Example environment variables
# Copy this to .env.local for local development (do NOT commit .env.local)
# The .env.local file is gitignored and will not be committed to GitHub

# Google Calendar private iCal URL (REQUIRED)
# Get this from: https://calendar.google.com/calendar/ical/.../private-.../basic.ics
# For production, set this as a GitHub Secret named VITE_CALENDAR_ICAL_URL
VITE_CALENDAR_ICAL_URL=https://calendar.google.com/calendar/ical/YOUR_CALENDAR_ID/private-YOUR_SECRET/basic.ics
